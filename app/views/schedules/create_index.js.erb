$(".body__rate<%= @passport.id.to_s %>").
  html('<%= j render partial: "passports/rate", locals: { passport: @passport } %>');
$(".body__rate<%= @passport.id.to_s %>").fadeIn(800);

$(".body__btn<%= @passport.id.to_s %>").
  html('<%= j render partial: "passports/schedule_form", locals: {passport: @passport, schedule: @schedule} %>');
